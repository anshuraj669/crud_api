{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/task_users_assignment/package.json",
                "localRoot": "${workspaceFolder}/task_users_assignment"
            }
        }
    ]
}